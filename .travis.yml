language: ruby
rvm:
 - 2.2.2
# uncomment and edit the following line if your project needs to run something other than `rake`:
gemfile:
  Gemfile
script:
  - rspec 'tests/run_rubocop.rb'
  - rspec 'tests/run_foodcritic.rb'
